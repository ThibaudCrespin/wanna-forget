export interface Memory {
    id: string;
    user: string;
    time: string;
    duration?: string;
    location: string;
    size?: string;
    with?: Number[];
    picture?: string;
    preview?: string;
    video: string;
}
